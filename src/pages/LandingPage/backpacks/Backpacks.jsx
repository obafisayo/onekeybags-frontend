import React from "react";
import styled from "styled-components";

function Backpacks() {
    return (
        <StyledDiv>
            This is the Backpacks page
        </StyledDiv>
    );
}

const StyledDiv = styled.div`
    
`;
export default Backpacks;
